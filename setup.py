from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='generate alert when wireless speed goes down.',
    author='shishant',
    license='',
)
